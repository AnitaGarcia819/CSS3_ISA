#include "assembly_program.h"
#include <vector>
#include <unordered_map>
using namespace std;
int main()
{

    Program program;
   /* vector<string> vec;
    vec.push_back("ADDM R1 M1 R2");
    vec.push_back("ADDR R2 R1 R3");

   // cout << vec[0] << endl;
    //cout << vec[1] << endl;

    pair<string, vector<string> > p;
    p.first = "LOOP";
    p.second = vec;

    program.functions.insert(p);


    for(int i = 0; i < program.functions["LOOP"].size();i++)
        cout << program.functions["LOOP"][i] << endl;
    */
    return 0;
}